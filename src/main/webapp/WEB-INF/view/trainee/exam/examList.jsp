<%@page pageEncoding="UTF-8"%><%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core"%><%@ taglib prefix="fn" uri="http://java.sun.com/jsp/jstl/functions"%><c:set var="ctx" value="${pageContext.request.contextPath}" /><c:set var="fdCategoryId" value="${fdCategoryId}" /><!DOCTYPE html><html lang="zh_CN"><head><script>	jQuery(document).ready(function() {		//为inputForm注册validate函数		$("#inputForm").validate();		return;	});</script></head><body>  <form class="form-horizontal" method="post" id="inputForm" action="${ctx}/group/exam/detailSave" name="form">    <input type="hidden" name="fdCategoryId" value="${fdCategoryId}" />    <section id="set-exam">      <div class="well">        <div class="page-header">          <a href="${ctx }/group/examCategory/list" class="tile-return">返回上页</a>        </div>        <table class="table">          <c:forEach items="${exams}" var="bean" varStatus="vstatus">            <table>              <tr>                <input type="hidden" name="fdId" value="${bean.fdId}" />                <td>${vstatus.index+1}&nbsp;&nbsp;${bean.fdSubject}</td>              </tr>              <c:forTokens items="${bean.fdOption}" delims="," var="item" varStatus="vs1">                <tr>                  <td><input <c:if test="${bean.fdType==0}"> type="radio"</c:if>                    <c:if test="${bean.fdType==1}"> type="checkbox"</c:if> name="fdAnswer_${bean.fdId}"                    value="${vs1.index+1}" class="required" />${item}</td>                </tr>              </c:forTokens>            </table>          </c:forEach>          <tr>            <td colspan="4"><button class="btn btn-info" type="submit" value="提交" id="saveBtn">确定</button></td>          </tr>        </table>      </div>    </section>  </form></body></html>